# Exemplo de variaveis de ambiente
# Copie para .env e configure

# Google Cloud Project (opcional se usando ADC)
# GOOGLE_CLOUD_PROJECT=your-project-id

# Porta do servidor (Cloud Run injeta automaticamente)
PORT=8080
